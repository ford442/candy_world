Oh! I amended my commit but `test-juke.js` was deleted? No, wait! I amended the commit with all the files created during the `npm run build` step (the compiled assets!). This is because I ran `git add . && git commit --amend`!
Oh no, I added hundreds of `dist/` and `test/` files to the git commit!
Let's fix this!
