# Swarm state — #1358 Batcher instance pose → matrix/color (arpeggio slice)

**Task:** Extract arpeggio-batcher matrix+color write loop into dedicated C++ export + TS wrapper with fallback. PlantPoseMachine stays in TS.
**Branch:** `cursor/batcher-instance-arpeggio-ecb0`
**Depends on:** #1351 parity harness (merged on main)
**C++ availability:** `em++` absent → native path SKIP; TS fallback always works

## Iteration 1 — Dedicated export + wrapper + wire arpeggio
- Status: Complete
- Files touched:
  - `emscripten/batcher_instance.cpp` — `batchWriteInstancePose_c` (TRS→matrix + optional color)
  - `emscripten/build.sh` / `exports.txt` — export manifest
  - `src/utils/wasm-batcher-instance.ts` — wrapper + `writeInstancePoseTS` fallback
  - `src/foliage/arpeggio-batcher.ts` — SoA colors + `writeInstancePose` flush
  - `assembly/batch.ts` — `batchWriteInstancePose` AS parity export
  - `tests/parity.mjs` Path 3 + `tests/parity/refs/write-instance-pose.mjs`
  - `tests/batcher-lod.test.mjs` — golden parity + Node microbench
  - `MIGRATION_TRACKER.md` — slice 1 status note
- Profile (task 1): Chrome perf unavailable headless (no GPU). Node microbench proxy:
  - n=50: ~0.027 ms/flush (~0.5 µs/instance)
  - n=200–500: ~0.002–0.006 ms/flush
- ABI: positions[3], quaternions[4], scales[3], colorsIn[3]? → matricesOut[16], colorsOut[3]?, intensity, count
- Verification:
  - `npm run build:wasm` ✅
  - `npm run build:emcc` ✅ SKIP (em++ absent)
  - `npm run test:parity` ✅ 16 PASS / 0 FAIL / 3 SKIP (incl. Path 3 pose)
  - `npm run test:batcher-lod` ✅
  - `npm run test:wasm` ✅
  - Touched TS: `wasm-batcher-instance.ts` 0 errors; arpeggio pre-existing TS issues unchanged
- Next: commit, push, PR
