# Swarm State — Gem Canopy Biome Finish & Ship (#1170)

> Status: **LANDED** — build/test green, capacity warnings resolved, docs updated.

## Mapping / Placement Status

| Component | Status | Notes |
|-----------|--------|-------|
| GemFruitBatcher (instanced gems) | ✅ | Shared geometry cloned per jewel type; `aPhase`/`aArmLen` isolated; `MAX_GEMS_PER_TYPE = getCIAdjustedCount(512, 0.1, 80)` |
| createGemCanopyTree | ✅ | Registers tree + gems via `onPlacement`; hit mesh + `userData.biome = 'gem_canopy'` |
| foliage-registry 'gem_canopy_tree' | ✅ | `batcherHint='gem_canopy'`, `supportsMusic=true` |
| generation-decorators corridor | ✅ | 24 trees along path (75,-115) → (125,-45) |
| generation-core calls corridor | ✅ | `populateGemCanopyCorridor(weatherSystem)` in `generateMap` |
| music-bindings gem_canopy block | ✅ | `shimmer [1,2]`, `hueShift [3]`, `noteColor [1]` |
| music-reactivity binding loop | ✅ | `MRState.gemCanopy*` parsed + `BiomeUniforms.gemCanopy` updated |
| sky_wave target_biomes | ✅ | `gem_canopy` added; moon melody hue now cascades to corridor |
| map-exporter SUPPORTED_EXPORT_TYPES | ✅ | `category='mushroom-trees'`, `biome='gem_canopy'` |
| batcher-telemetry | ✅ | id='gem_canopy', drawCalls = 3 (one per jewel type) |
| world-health | ✅ | Gems counted via batcher telemetry; `gem_canopy_tree` spawn attempts tracked |

## Files Modified

- `src/foliage/gem-fruit-batcher.ts` — clone geometry per InstancedMesh, tune Crystal emissive/rim, add Visual/Music Impact comments, capacity tuned.
- `src/foliage/gem-canopy-tree.ts` — signature bubble-willow tree factory with hanging gems.
- `src/world/generation-decorators.ts` — `populateGemCanopyCorridor` (24 trees + 6 accent trees).
- `assets/music-bindings.json` — added `gem_canopy` to `sky_wave.target_biomes`.
- `src/core/config.ts` — added `gem_canopy` jewel-tone note color map.
- `docs/MUSIC_MAP_BINDING.md` — Gem Canopy binding section.
- `src/utils/startup-profiler.ts` — fixed duplicate `toggleOverlay` export (build blocker).
- `src/utils/wasm-loader-cpp.ts` — fixed duplicate `setEmscriptenInstance` export (build blocker).

## Build & Test

| Command | Result |
|---------|--------|
| `npm run build:ci` | ✅ pass |
| `npm run test:wasm` | ✅ pass |
| `npm run test` (CORE smoke) | ✅ pass (`__sceneReady`, jukebox UI) |
| `FULL_BOOT=fast npm run test` | ✅ pass (24 gem_canopy_trees spawned, 2322/2322 objects, zero GemFruitBatcher capacity warnings) |
| `RENDERER=webgl npm run test` | ✅ pass (WebGL fallback boots cleanly) |

## Verification Notes

- CI/headless runs with `?safe=1`, so shader warmup and compute are disabled (expected).
- Optional Emscripten native module unavailable in this environment; JS fallbacks active (expected).
- Full WebGPU shader validation requires a real GPU environment; deferred to manual/GPU testing.

## Remaining / Follow-ups

- None for #1170.
