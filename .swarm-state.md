# Swarm State — Music-Reactive Atmosphere Bridge (#1169)

## Mapping Status

| Row | Signal | Target Uniform | Status |
|-----|--------|----------------|--------|
| 1 | Kick / bass energy (`atmosphere.bloom.channels` [0,6]) | `uBloomStrength` (rest 1.0 → peak 2.5) | **verified** |
| 2 | Mix energy / avg volume | `uCrescendoFogDensity` (candy-dream cap 0.85) | **verified** |
| 3 | Melody (`sky_moon.melody_channel`) | `uShaftOpacity` + night shaft visibility | **verified** |
| 4 | BeatSync downbeats | Bloom spike + shaft shimmer (smooth decay) | **verified** |
# Swarm State — Issue #1265: Player Ground Level, Eye Height & Object Alignment

> Started: 2026-06-30
> Status: **SLICES 1–4 COMPLETE** — awaiting final verification on hardware with stable WebGPU.

## What We’re Solving

Unify and harden ground-height computation so the first-person camera stays smooth and batched instances (trees, mushrooms, roots/rocks) sit flush on the ground. This is the foundation for #1266 (walkable cloud platforms).

## Current Findings

- ✅ **Ground query centralized**: `src/systems/ground-system.ts` is now the single source for terrain + lake/island + platform overrides + cached height queries.
- ✅ **Lake constants unified**: `LAKE_BOUNDS`, `LAKE_BOTTOM`, `LAKE_ISLAND`, `isInLakeBasin`, `isOnLakeIsland` live in GroundSystem and are re-exported for compatibility.
- ✅ **Consumers routed**: `physics.core.ts`, `physics-core.ts`, `physics-states.ts`, `physics-updates.ts`, `camera-modes.ts`, `game-loop.ts`, `glitch-grenade.ts`, `rainbow-blaster.ts`, `main.ts`, `ground-heightmap.ts`, and `generation-utils.ts` all read from GroundSystem.
- ✅ **Eye height data-driven**: `CONFIG.player.eyeHeight` / `spawnEyeHeightY` replace hard-coded `1.8` and `5`; `CONFIG.ground.followLerpSpeed` / `followMaxStep` drive camera vertical smoothing.
- ✅ **C++ Y post-processed**: after `updatePhysicsCPP`, player Y is reconciled against the authoritative GroundSystem height.
- ✅ **Batcher placement verified**: world generation already calls `getUnifiedGroundHeight()` (delegated to GroundSystem); no manual `y + 0.1` hacks remain for trees/mushrooms.
- ✅ **Debug visualizer added**: `src/debug/ground-debug.ts` with `?debugHeights=1` / `?debugPlayer=1` overlays.
- No `RockBatcher` currently exists in the repo.

## Completed Work

1. ✅ Created `src/systems/ground-system.ts` as the single authoritative ground query (terrain + lake/island + spatial cache + platform hook).
2. ✅ Routed all main-thread ground consumers through it; workers still have their own constants (known limitation).
3. ✅ Added `CONFIG.player.eyeHeight` / `spawnEyeHeightY` and `CONFIG.ground.followLerpSpeed` / `followMaxStep`; replaced Y snaps with smooth lerp + max-step clamp.
4. ✅ Verified base-Y anchoring in `TreeBatcher` / `MushroomBatcher` via world-generation `getUnifiedGroundHeight()` delegation.
5. ✅ Added `src/debug/ground-debug.ts` with `?debugHeights=1` / `?debugPlayer=1` overlays.
6. ✅ `npm run build:ci` and `npm run test:wasm` green. Headless browser smoke tests fail due to SwiftShader WebGPU device loss (documented VM limitation).

## Build / Test Status

| Command | Status | Notes |
|---------|--------|-------|
| `npm run build:ci` | ✅ green | WASM + Vite build passes |
| `npm run test:wasm` | ✅ green | Particle bounds tests pass |
| `npm run test` | ⚠️ fails | WebGPU device lost in headless SwiftShader VM; unrelated to these changes |
| `FULL_BOOT=fast npm run test` | ⚠️ fails (exit 0) | Same SwiftShader instability; runner ignores FULL_BOOT failures |
| `RENDERER=webgl npm run test` | ⚠️ fails | Scene-ready timeout in headless VM |

## Next Steps

1. Run smoke tests on a machine with real WebGPU/WebGL to confirm `window.__sceneReady` and jukebox UI assertions pass.
2. Verify visually that the camera no longer snaps over small terrain bumps and that trees/mushrooms sit flush on the lake island.
3. ✅ Landed #1266 walkable cloud platforms on top of the new `registerPlatform` / `unregisterPlatform` hooks.

## Files Modified

- `src/systems/ground-system.ts` (new)
- `src/systems/physics.core.ts`
- `src/systems/physics/physics-core.ts`
- `src/systems/physics/physics-states.ts`
- `src/systems/physics/physics-updates.ts`
- `src/systems/physics/physics-types.ts`
- `src/core/camera-modes.ts`
- `src/core/game-loop.ts`
- `src/core/init.ts`
- `src/core/main.ts`
- `src/core/deferred-init.ts`
- `src/gameplay/rainbow-blaster.ts`
- `src/particles/compute-particles.ts`
- `src/world/generation-utils.ts`
- `src/world/ground-heightmap.ts`
- `src/foliage/lod.ts`
- `src/rendering/culling/culling-system.ts`
- `src/debug/ground-debug.ts` (new)
- `src/core/config.ts`
- `.swarm-state.md`

## Open Questions / Risks

- WebGPU smoke tests cannot be validated in this headless VM; must be checked on real hardware before merge.
- GPU heightmap vs CPU query mismatch may need follow-up tuning; out of scope for this issue unless it blocks acceptance.

---

# Swarm State — Gem Canopy Biome Finish & Ship (#1170)

> Status: **LANDED** — build/test green, capacity warnings resolved, docs updated.

## Mapping / Placement Status

| Component | Status | Notes |
|-----------|--------|-------|
| GemFruitBatcher (instanced gems) | ✅ | Shared geometry cloned per jewel type; `aPhase`/`aArmLen` isolated; `MAX_GEMS_PER_TYPE = getCIAdjustedCount(512, 0.1, 80)` |
| createGemCanopyTree | ✅ | Registers tree + gems via `onPlacement`; hit mesh + `userData.biome = 'gem_canopy'` |
| foliage-registry 'gem_canopy_tree' | ✅ | `batcherHint='gem_canopy'`, `supportsMusic=true` |
| generation-decorators corridor | ✅ | 24 trees along path (75,-115) → (125,-45) |
| generation-core calls corridor | ✅ | `populateGemCanopyCorridor(weatherSystem)` in `generateMap` |
| music-bindings gem_canopy block | ✅ | `shimmer [1,2]`, `hueShift [3]`, `noteColor [1]` |
| music-reactivity binding loop | ✅ | `MRState.gemCanopy*` parsed + `BiomeUniforms.gemCanopy` updated |
| sky_wave target_biomes | ✅ | `gem_canopy` added; moon melody hue now cascades to corridor |
| map-exporter SUPPORTED_EXPORT_TYPES | ✅ | `category='mushroom-trees'`, `biome='gem_canopy'` |
| batcher-telemetry | ✅ | id='gem_canopy', drawCalls = 3 (one per jewel type) |
| world-health | ✅ | Gems counted via batcher telemetry; `gem_canopy_tree` spawn attempts tracked |

## Files Modified

- `src/systems/atmosphere-reactivity.ts` — **NEW** dedicated atmosphere stage (zero-alloc update, beat pulse, map overrides)
- `src/systems/music-reactivity.ts` — calls `updateAtmosphereReactivity`, registers beat sync, exports `AtmosphereShaftState`
- `src/core/game-loop.ts` — re-enabled golden-hour shafts (frustum-gated), night moonbeams, WebGL opacity parity
- `src/core/init.ts` — stores shared `shaftMaterial` on `lightShaftGroup.userData` for WebGL sync
- `assets/music-bindings.json` — added top-level `atmosphere` section
- `docs/MUSIC_MAP_BINDING.md` — documented atmosphere bindings + runtime flow

## Build & Test (last run)

| Command | Result |
|---------|--------|
| `npm run build:ci` | pass |
| `npm run build` | pass (emcc wasm-opt flake; vite green) |
| `npm run test:wasm` | pass |
| `npm run test:smoke:fast` | `__sceneReady` reached; full-population timeout is known headless flake (CI exits 0) |

## Open Decisions

- **Separate module**: `atmosphere-reactivity.ts` created (not inlined into music-reactivity.ts).
- **Bloom tuning**: rest `1.0`, peak `2.5`, nightGate `0.35–1.0` via dayNightBias.
- **Fog tuning**: scale `0.65`, max `0.85` (candy-dream, not murky).
- **Shaft cap**: opacity max `0.4` day / `0.35` night; frustum dot threshold `0.28`.
- **Live shaft module**: `src/core/init.ts` (not `init.js`).
- **WebGL shafts**: `game-loop.ts` mirrors `uShaftOpacity` → `lightShaftGroup.userData.shaftMaterial.opacity`.

## Resume Here

All four mapping rows wired and green on build:ci + test:wasm + smoke `__sceneReady`. Optional: run full `npm run test` in a WebGPU-capable environment for bloom-response visual regression baseline.
