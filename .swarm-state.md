# Swarm state — Circadian day/night across all instanced batchers (#1362)

## Iteration 0 — Baseline verification (pre-change)
- Status: Complete
- Files touched: none (verification only)
- Verification:
  - `npm run build:ci` ✅
  - `npm run test:wasm` ✅
  - `npm run test` ✅
  - `FULL_BOOT=fast npm run test` ✅
  - `RENDERER=webgl npm run test` ✅

## Iteration 1 — Update Circadian logic for Batchers
- Status: Complete (100%)
- Files touched:
  - `src/core/game-loop-visuals.ts` — updated `circadianController` usage to appropriately pass the delta and day night configuration instead of a constant.
  - `src/foliage/mushroom-batcher.ts` — verified and documented use of `uCircadianPhase` for glow adjustments.
  - `src/foliage/luminous-plant-batcher.ts` — verified and documented use of `uCircadianPhase` for glow adjustments.
- Verification:
  - `npm run build:ci` ✅
  - `npm run test:wasm` ✅
  - `npm run test` ✅
  - `FULL_BOOT=fast npm run test` ✅
  - `RENDERER=webgl npm run test` ✅
- Notes: Pre-existing implementation within most batchers, just required `circadianController` fixes in the core game-loop visual step to animate. Tested and built locally successfully.
- Next step: Sky Islands Biome (#1363) or the app-chunk split (#1361).
