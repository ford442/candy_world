# Swarm State — Issue #1347 (Typecheck Ratchet + Burndown)

## Iteration: 3 (FINAL)
**Status:** COMPLETE — ratchet infra live, baseline lowered, all three clusters resolved, verification green.

## Issue
- #1347 — TypeScript typecheck gate + error-count ratchet
- #1350 — music-reactivity module split (bundled)
- #1357 — foliage-registry dead import (bundled)

## TSC Error Count
| Stage | Count | Delta |
|-------|-------|-------|
| Seeded baseline (clean install) | 573 | — |
| After #1357 (foliage-registry) | 572 | -1 |
| After #1350 (music-reactivity split) | ~551 | -21 |
| After game-loop null-safety | 550 | -3 |
| **Committed baseline** | **550** | **-23 total** |

## Files Touched

### Ratchet infrastructure (iteration 1)
- `package.json` — added `typecheck`, `typecheck:ratchet` scripts
- `scripts/tsc-ratchet.mjs` — NEW: runs `tsc --noEmit`, compares to baseline, fails on regression
- `scripts/tsc-baseline.json` — NEW: committed baseline (`maxErrors: 550`)
- `.github/workflows/typecheck.yml` — NEW: PR gate on `main` via `pnpm install --frozen-lockfile` + ratchet

### #1357 — foliage-registry (iteration 2)
- `src/world/foliage-registry.ts` — removed dead `createSubwooferLotus` import (replaced by `subwooferLotusBatcher` proxy registration)

### #1350 — music-reactivity module split (iteration 2)
- `src/systems/music-reactivity-core.ts` — canonical implementation: `MRState`, helpers, `WeatherMusicTargets`, `computeWaveDistSq`, `MusicReactivitySystem`, atmosphere integration
- `src/systems/music-reactivity.ts` — thin re-export barrel:
  ```ts
  export * from './music-reactivity-core.ts';
  export { AtmosphereShaftState } from './atmosphere-reactivity.ts';
  ```
- Removed ~840 lines of duplicated local declarations that collided with line-1 imports (TS2440/TS2459/TS2395 cluster)

### game-loop null-safety (iteration 3)
- `src/core/game-loop.ts` — guarded `sunGlowRef`/`sunCoronaRef`/`moonRef` position/visibility updates in day/night cycle branches

## Verification (all green)
| Command | Result |
|---------|--------|
| `node scripts/tsc-ratchet.mjs` | ✅ 550/550 |
| `npm run build:ci` | ✅ |
| `npm run test:wasm` | ✅ |
| `npm run test:atmosphere` | ✅ 8/8 |
| `RENDERER=webgl npm run test` | ✅ PASSED (`window.__sceneReady`) |

## Not touched (per scope)
- `emscripten/**`, `vite.config.js`, batchers, `music-reactivity.core.ts` (legacy calc helpers — 2 geometry errors remain)
- `src/workers/worker-pool.ts` (deferred — 4 errors remain, not in top clusters)

## Next step
None required for this PR. Future burndown candidates:
1. `src/workers/worker-pool.ts` — `Function` resolve/reject + `PhysicsRequest` payload shapes (~4 errors)
2. `src/foliage/types.ts` — extend `FoliageObject` with `geometry` or narrow culling casts (~4 errors in music-reactivity-core + .core)
3. `ChannelData.note` string→number assignments in biome bindings (~5 errors)
4. TSL `ShaderNodeObject.copy` in game-loop (~7 errors, outside sun/moon scope)

To lower baseline after future fixes:
```bash
npx tsc --noEmit 2>&1 | grep -c "error TS"
node scripts/tsc-ratchet.mjs --set-baseline <new-count>
# commit scripts/tsc-baseline.json
```