# Swarm State — Lint/Typecheck Ratchet Hardening

**Last updated:** 2026-08-04 (iteration 1 complete)

## Current Honest Counts

| Gate | Started (committed) | Honest at audit | **Now** | Delta from honest |
|------|--------------------:|----------------:|--------:|------------------:|
| TypeScript errors | 206 | 229 | **220** | −9 |
| ESLint problems | 2451 | 2747 | **2689** | −58 |

Baselines in `scripts/tsc-baseline.json` and `scripts/eslint-baseline.json` match a clean-checkout run.

## Gate Status — PROVEN FAIL-CLOSED ✅

| Workflow | Gate command | Blocks regression? | How tested |
|----------|-------------|-------------------|------------|
| `typecheck.yml` | `npm run typecheck` | **YES** | Set baseline 228 (< actual 229) → exit 1 |
| `lint.yml` | `npm run lint` | **YES** | Set baseline 2746 (< actual 2747) → exit 1 |

### Changes made to gates
- `lint.yml`: removed `continue-on-error: true` and `--report-only`; ratchet is now the blocking step
- `package.json`: `"lint"` → `node scripts/eslint-ratchet.mjs` (added `lint:raw` for human output)
- `eslint-ratchet.mjs`: auto-ratchets baseline down on improvement (symmetric with `tsc-ratchet.mjs`)
- Baselines corrected to honest counts before burn-down (206→229 TSC, 2451→2747 ESLint)

## Clusters Completed

### Cluster 1 — utils + analytics (6 files)
- `src/utils/seeded-random.ts` — console → log
- `src/utils/bootstrap-loader.ts` — console → log; removed duplicate `Window.setLoadingStatus` decl (TS2687)
- `src/utils/geometry-dedup.ts` — console → log; added `uniqueGeometries: 0` (TS2741)
- `src/utils/background-processor.ts` — console → log
- `src/utils/startup-profiler.ts` — removed duplicate util fns (TS2440 ×4)
- `src/systems/analytics/core.ts` — console → log.debug/warn

**Result:** TSC 229→224, ESLint 2747→2717

### Cluster 2 — UI loading screen leaves (5 files)
- `src/ui/announcer.ts` — removed duplicate type re-export (TS2484 ×4); console → log
- `src/ui/loading-screen-types.ts` — phase callbacks console → log.debug
- `src/ui/loading-screen-ui.ts` — console → log
- `src/ui/loading-screen-progress.ts` — console → log
- `src/ui/loading-screen-reporting.ts` — console.group/table → log.debug

**Result:** TSC 224→220, ESLint 2717→2689

## Verification (all green ✅)
```
npm run build:wasm   # OK
npm run build        # OK
npm run test:wasm    # 3/3 passed
npm run typecheck    # 220/220
npm run lint         # 2689/2689
```

## Next Clusters (not started)
- `src/ui/analytics-debug-handlers.ts` — console hijack for debug overlay (intentional?)
- `src/utils/wasm-loader-core.ts` — WasmImportObject typing (TS2345, not trivial)
- `src/utils/wasm-*.ts` — many console calls, higher coupling

## Deferred — Need Noah / Out of Scope
- `src/foliage/**`, `src/rendering/**`, `src/world/**`, `src/particles/**`, `src/compute/**` — forbidden (#1496)
- `src/audio/**`, `src/core/**`, `src/workers/**` — bulk of remaining 220 TSC errors; not leaf-safe
- Remaining 220 TSC errors are mostly TSL shader types, missing symbols in world/generation, ChannelData shape drift — mechanical fixes risk behaviour changes

## Footguns Found
1. **Stale baselines on main** — TSC 206 vs 229, ESLint 2451 vs 2747 meant gates were broken/misleading
2. **lint.yml was explicitly warn-only** — `continue-on-error: true` + `--report-only`
3. **`npm run lint` ran bare eslint** — never invoked ratchet locally
4. **TSC ratchet auto-writes baseline on improvement** — ESLint now matches; both will commit lowered baselines in the same PR as fixes
