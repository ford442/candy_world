# Swarm State — CI signal repair (2026-09-23)

_Overwrites the completed 2026-09-02 character-controller run. That content is
gone; `git log --follow .swarm-state.md` has it if ever needed._

**Task:** get ONE green `Node protocol tests` run that is capable of failing.
**Branch:** `claude/busy-bell-8gx1sh`, based on `origin/main` @ `6fe2bfa`.
**Boundary honoured:** `git diff --stat` shows **zero files changed under `src/`**.

---

## Iteration 1 — ground truth, verified not assumed

Environment note first, because it changes what "verified" means: the container
shipped Node **v22.22.2** and **no `node_modules`**. CI uses Node **24**. The
bug in question is Node-24-specific, so I installed Node 24.21.0 via
`/opt/nvm` and ran everything below under it. On Node 22 the failing script
**passes** — anyone reproducing on the default container Node will wrongly
conclude there is no bug.

```
$ nvm install 24 && node -v
v24.21.0
$ pnpm install --frozen-lockfile     # node_modules was absent
Done in 4.4s using pnpm v10.33.0
```

### Defect 1 — CONFIRMED, and still live on current main

`main` has moved past `90bd6c1` (the brief's HEAD) to `6fe2bfa`. Run 466
(`node-protocol-tests.yml`, push, head_sha `6fe2bfa`) is still `failure`.
Fetched log, verbatim:

```
TypeError [ERR_INVALID_URL]: Invalid URL
    at getSourceSync (node:internal/modules/esm/load:45:13)
  code: 'ERR_INVALID_URL',
  input: 'data:text/javascript;base64,ZXhwb3J0IGRlZmF1bHQge307?init'
```

Reproduced locally under Node 24 — identical stack, identical `input`.

Chain confirmed on disk: `src/utils/wasm-loader-core.ts:29` does
`import initCandyPhysics from '../wasm/candy_physics.wasm?init'`;
`tests/support/css-hooks.mjs` matched it and returned a `data:` URI; a loader
further down the chain (tsx) re-attaches the `?init` search to whatever URL the
resolve hook returns. `data:...?init` is rejected by Node 24's ESM loader.
Note `new URL('data:...?init')` parses **fine** on Node 22 — I checked — which
is exactly why this regressed silently.

**Regression, not pre-existing:** the brief says `88b2673` was green on this
workflow. I did not confirm that specific claim (run 466 and the six runs
listed by the API are all `failure`; I did not page back far enough to find the
last green `node-protocol-tests` run). **Recorded as unverified.** It does not
change the fix.

### Defect 2 — CONFIRMED

`test:integration` ended `... && vite build && npm run test && npm run
test:smoke:fast`. Both `test` and `test:smoke:fast` run
`tests/smoke-runner.mjs` (Playwright Chromium + WebGPU).
`.github/workflows/node-protocol-tests.yml` has exactly three steps before the
test step — checkout, pnpm setup, `pnpm install --frozen-lockfile`. No
`playwright install`, no `--enable-unsafe-webgpu`. It cannot pass there.

### Defect 3 — CONFIRMED, and **worse than the brief states**

The brief says only `test:save-entity-snapshot` of the four deferred legs is
defined. The npm script is defined, but the file it points at **does not
exist**:

```
$ npm run test:save-entity-snapshot
Error [ERR_MODULE_NOT_FOUND]: Cannot find module
  '/home/user/candy_world/tests/save-entity-snapshot-roundtrip.test.mjs'
```

And the brief's claim that `tests/character-controller-native.test.mjs` "DOES
exist (it arrived with PR #1771)" is **false**:

```
$ ls tests/ | grep -iE "save|character"
character-controller.test.mjs
```

Only `character-controller.test.mjs` is there. So **all four** legs of
`test:integration:deferred` were dead, not three.

### Defect 4 — CONFIRMED but MIS-DIAGNOSED in the brief

Visual regression is **not** uniformly red. Pull-request runs 1984, 1986, 1987,
1988, 1989 are all `success`. Only the `push`-to-`main` runs fail, and it is
the second job, not the first. Run 1985 job breakdown: `visual-regression` =
success, `update-baselines` = failure at step 10. Its log:

```
The following paths are ignored by one of your .gitignore files:
tools/visual-regression/test/baselines
hint: Use -f if you really want to add them.
##[error]Process completed with exit code 1.
```

`tools/visual-regression/.gitignore:3` is `test/baselines/` — the exact path
`git add` targets. Confirmed with `git check-ignore -v`.

The deeper finding, which is the reason I disabled rather than patched: because
baselines are gitignored, **not one baseline image is committed**
(`git ls-files tools/visual-regression/test` → empty), and
`tools/visual-regression/cli.ts:190` handles a missing baseline with
`if (!baseline) { warn; continue; }`. So the PR job performs **zero
comparisons** and exits 0 regardless. Its uploaded artifact on run 1989 is
"1 file / 2892 bytes" — the report JSON, no diffs. A one-line `git add -f`
would have turned the workflow green while leaving a check that cannot fail,
which is the precise defect this task exists to remove.

### Defect 5 — CONFIRMED (partially; the brief overstates "silent")

`test:parity` did already print `⏭ C++ SKIP` lines and `3 SKIP` in its tally.
What it did not do was name the missing artifact or qualify its final line,
which read a flat `Parity harness green.`

---

## Iteration 2 — what I changed and why

| file | change |
| --- | --- |
| `tests/support/empty-module.mjs` | **new.** A real on-disk stub exporting `{}`. |
| `tests/support/css-hooks.mjs` | resolve asset imports to that file's `file:` URL instead of a `data:` URI. `file:///...?init` is a legal URL; `data:...?init` is not. |
| `scripts/check-docs-symbols.mjs` | **new guard.** Imports the real runtime `CONFIG` and checks every `CONFIG.a.b` cited in `docs/**` resolves. |
| `scripts/check-plan-integrity.mjs` | **new guard.** No *filled* `Outcome:` line in `weekly_plan.md` may repeat. |
| `package.json` | added `test:fast` (PR gate, node-only), `test:docs-symbols`, `test:plan-integrity`; `test:integration` = `test:fast` + `vite build` + smoke; **deleted** `test:integration:deferred` and `test:save-entity-snapshot`. 5-line diff, no dependency touched. |
| `.github/workflows/node-protocol-tests.yml` | one step, `pnpm run test:fast`, with a comment saying why smoke is not here. |
| `.github/workflows/visual-regression.yml` | reduced to `workflow_dispatch`, dated header comment with the evidence above and a three-point re-enable checklist. |
| `tests/parity.mjs` | reporting only — records every missing `candy_native*` artifact and prints a `!! SKIPPED:` block; final line now says "green for the tiers that ran (TS↔AS). 3 tier(s) SKIPPED". Still exits 0. |
| `docs/CI_CONTRACT.md` | **new.** Which suite gates what, and the honest table of what was dropped. |

Design note on `check-plan-integrity.mjs`: a literal "no `Outcome:` line twice"
rule **fails on main today** — `Outcome: <!-- fill in at end of day after
kimi-cli loop -->` legitimately appears 8 times (10 after `6fe2bfa`). The guard
exempts placeholders whose body is empty once HTML comments are stripped, and
anchors on line start so prose that merely mentions "Outcome:" mid-sentence is
not matched. It checks exactly the failure mode #1724 exhibited: a real string
replicated across historical entries.

Why `test:integration:deferred` was deleted rather than completed: two of its
four legs had no test file to point at. Writing a save-entity-snapshot
round-trip means testing `src/systems/save-system/**`, which a parallel Copilot
slice owns today. The drop is recorded in `docs/CI_CONTRACT.md` with a table of
each leg's verified state.

---

## Iteration 3 — mutation proofs (every one run, none assumed)

**Guards (subject mutation — the real thing):**

| guard | mutation | result |
| --- | --- | --- |
| `test:docs-symbols` | appended `CONFIG.candyNonsense.knobThatDoesNotExist` to `docs/ACCESSIBILITY.md` | exit **1**, named the symbol and the citing file. Reverted. |
| `test:plan-integrity` | duplicated the line `Outcome: Phase 1 (GC Spike elimination...)` in `weekly_plan.md` | exit **1**, reported `lines 451, 452`. Reverted (`git checkout --`; `weekly_plan.md` is byte-identical). |

**All 31 node-only scripts (failure-surfacing mutation):** injected
`throw new Error("MUTATION-CHECK: ...")` into each test file and required a
non-zero exit. First pass appended the throw; 10 scripts still exited 0 —
**not a defect**: those files end with a top-level `process.exit(...)`, so the
appended line is unreachable. Re-ran those 10 with the throw *prepended*:

```
CAN-FAIL(prepend) test:atmosphere  test:budgets  test:fauna  test:foliage-interact
CAN-FAIL(prepend) test:ground  test:ground-unified  test:joint-fallback
CAN-FAIL(prepend) test:presence  test:sky-islands  test:sugar-caves
```

**31 of 31 exit non-zero when their subject file fails.** `git status tests/`
clean afterwards.

**Breadth probe (genuine subject mutation):** multiplied every numeric literal
in `src/core/config/defaults.ts` by 3.7 and re-ran all 31. Five went red:
`test:cascades`, `test:clustered`, `test:gi`, `test:lights`, `test:postfx`.
The other 26 target subjects that file does not feed, so this is expected —
recorded as evidence of live wiring for those five, not as a defect in the
rest. `src/core/config/defaults.ts` restored; `git status src/` clean.

**Defect-1 fix proven bidirectionally:** `test:entity-snapshot` fails with
`ERR_INVALID_URL` before the hook change and passes after, both on Node 24,
same command.

---

## Iteration 4 — final local state

```
$ npm run test:fast          # Node 24, on origin/main 6fe2bfa + this branch
EXIT=0     real  0m19.9s
$ npm run typecheck
Current errors: 0   Baseline errors: 0   ✅
$ npm run lint
ESLint problems: 1959 (0 errors, 1959 warnings) — baseline: 1959, delta: +0  ✅
$ git diff --stat -- src/
(empty)
```

**CI, observed (not predicted):** PR #1802, run 467 on head `e1baa7e` →
`conclusion: success`, 25 s. Five checks green: `protocol`, `typecheck`,
`lint`, `budget`, `manifest-lint`.

`Node protocol tests` would catch **any regression in the pure-logic layer —
WASM/physics parity, world and entity data contracts, rendering configuration —
plus docs citing config knobs that do not exist.**

---

## Still broken or unproven

1. **The smoke suite has never been run by me.** `npm run test` /
   `test:smoke:fast` need Playwright Chromium with WebGPU; I did not install a
   browser and did not execute them. `test:integration` as a whole is therefore
   **unverified end to end** — only its `test:fast` prefix is proven. Do not
   claim it passes.
2. **`vite build` unverified.** Not run this session. It is in
   `test:integration`, not in the PR gate, so it cannot redden the gate.
3. **"88b2673 was green" unconfirmed** — see Defect 1 above.
4. **The C++/Emscripten parity tier has still never executed anywhere.** It is
   now loud about it, but 3 comparison groups remain unchecked against C++ and
   will stay that way until someone builds `public/candy_native*.wasm`.
5. **Visual regression is disabled, not fixed.** Nothing verifies rendering
   output. That is a real coverage hole, deliberately chosen over a green check
   that compares nothing.
6. **Assertion-level wiring is proven for 7 of 31 scripts** (2 guards +
   `test:entity-snapshot` + the 5 config-sensitive rendering suites, minus
   overlap). For the other ~24 I proved only that a failure inside the file
   surfaces as a non-zero exit — **not** that each individual assertion is
   coupled to live `src/` behaviour. If another phantom suite is hiding in this
   repo, that is where to look.
7. ~~No CI run of this branch observed yet.~~ **RESOLVED.** PR #1802, run 467
   (`node-protocol-tests.yml`, head `e1baa7e`): **`success` in 25 s** — the
   first green `Node protocol tests` in this sequence. All five PR checks are
   green (`protocol`, `typecheck`, `lint`, `budget`, `manifest-lint`), and
   `Visual Regression Tests` correctly did not run.

---

## EXACT NEXT STEP for a cold reader

You are on branch `claude/busy-bell-8gx1sh`, based on `origin/main` @ `6fe2bfa`.

1. **Use Node 24, not the container default.** `export NVM_DIR=/opt/nvm;
   . /opt/nvm/nvm.sh; nvm use 24`. Then `pnpm install --frozen-lockfile` if
   `node_modules` is missing. On Node 22 the original bug does not reproduce.
2. **The PR gate is already green** — run 467 on `e1baa7e`, 25 s, all five
   checks. If a later run goes red, read the step log before changing
   anything: the whole point is that this job is now able to fail.
3. **Then, and only then, pick up item 1 or 6 from "Still broken or
   unproven".** Item 6 is the higher-value one: take any suite from the
   `test:fast` chain, change the `src/` function it claims to cover, and see
   whether it notices. Record the result here.
4. **Do not re-add `vite build` or the smoke runs to
   `.github/workflows/node-protocol-tests.yml`** without first adding
   `npx playwright install --with-deps chromium` and `--enable-unsafe-webgpu`,
   and proving one green run. That omission is what started this task.
