# Orphaned Batcher Wiring — Swarm State

## Plan

| Batcher | BiomeId | Reasoning |
|---------|---------|-----------|
| aurora.ts | `sky_moon` | Atmospheric sky effect; semantically part of the sky/moon system. |
| arpeggio.ts | — | No material/TSL node created here; pure CPU animation logic. Skipped. |
| arpeggio-batcher.ts | `arpeggio_grove` | ALREADY WIRED (uses getBiomeUniforms + shimmer/hueShift). |
| chromatic.ts | `global` | Full-screen post-process overlay; not tied to any local biome. |
| panning-pads.ts | `arpeggio_grove` | Ground pads in the grove; reuse existing grove uniforms. |
| silence-spirits.ts | `crystalline_nebula` | Ethereal, ghostly beings fit the crystalline/nebula aesthetic. |
| waterfall-batcher.ts | `crystalline_nebula` | Shimmering water; crystalline shimmer maps well to water sparkle. |
| musical_flora.ts | `arpeggio_grove` | Flora (retrigger mushroom glitch material) lives in the grove. |
| lake_features.ts | `crystalline_nebula` | Creek water; same shimmer logic as waterfall. |

## New BiomeIds needed
- `global` — for screen-wide effects (chromatic pulse). Added to BiomeUniforms, BiomeId, getBiomeUniforms, music-bindings.json, and music-reactivity.ts routing.
- `sky_moon` — already exists in BiomeUniforms but missing from BiomeId type and getBiomeUniforms helper; adding for completeness so aurora.ts can use the helper.

## Final summary
Batchers wired: 7 (aurora.ts, chromatic.ts, panning-pads.ts, silence-spirits.ts, waterfall-batcher.ts, musical_flora.ts, lake_features.ts)
New BiomeIds added: `global` (for screen-wide effects)
music-bindings.json entries added: `global` under `biomes` with shimmer [6], hueShift [7], noteColor [6]
Other BiomeIds used: `sky_moon` (aurora), `arpeggio_grove` (panning-pads, musical_flora), `crystalline_nebula` (silence-spirits, waterfall-batcher, lake_features)
npm run build: pass
npm run test:wasm: pass

## Status

### Foundation updates
- [x] biome-uniforms.ts: add `sky_moon` + `global` to BiomeId, add `global` uniform group, update getBiomeUniforms
- [x] music-bindings.json: add `global` entry under biomes
- [x] music-reactivity.ts: add channel routing for `global`
- [x] Pre-existing build blocker fixed in `src/ui/loading-screen.ts` (duplicate LoadingScreen declaration)

### Batcher wiring
- [x] aurora.ts — `sky_moon`
  - Uniform(s) wired: `noteColor` (aurora curtain finalColorRGB +0.12 mul), `noteColor`+`shimmer` (harmony orb emissive +0.25 mul)
  - music-bindings.json: no change needed (sky_moon already exists)
  - Build: pass
- [x] chromatic.ts — `global`
  - Uniform(s) wired: `noteColor` + `shimmer` (added to final overlay color, scaled by uChromaticIntensity * 0.15)
  - music-bindings.json: no change needed (global added in foundation)
  - Build: pass
- [x] panning-pads.ts — `arpeggio_grove`
  - Uniform(s) wired: `noteColor` + `shimmer` (glow disc emissive boost, mul 0.5)
  - music-bindings.json: no change needed
  - Build: pass
- [x] silence-spirits.ts — `crystalline_nebula`
  - Uniform(s) wired: `noteColor` + `shimmer` (emissive tint, mul 0.35)
  - music-bindings.json: no change needed
  - Build: pass
- [x] waterfall-batcher.ts — `crystalline_nebula`
  - Uniform(s) wired: `noteColor` + `shimmer` (column emissive tint, mul 0.3)
  - music-bindings.json: no change needed
  - Build: pass
- [x] musical_flora.ts — `arpeggio_grove`
  - Uniform(s) wired: `noteColor` + `shimmer` (glitch mushroom emissive tint, mul 0.25)
  - music-bindings.json: no change needed
  - Build: pass
- [x] lake_features.ts — `crystalline_nebula`
  - Uniform(s) wired: `shimmer` (creek emissive bioluminescence, mul 0.35)
  - music-bindings.json: no change needed
  - Build: pass

### Skipped / already wired
- [x] arpeggio.ts — No material/TSL nodes in this file; pure CPU animation logic. Skipped.
- [x] arpeggio-batcher.ts — Already wired to `arpeggio_grove` (uses `getBiomeUniforms('arpeggio_grove')`, consumes `shimmer` + `hueShift`).

