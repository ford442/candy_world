# Swarm state — Props sit on the ground (grounding cluster)

## Iteration 0 — Baseline verification (pre-change)
- Status: Complete
- Files touched: none (verification only)
- Verification:
  - `npm run build:ci` ✅
  - `npm run test:wasm` ✅
  - `npm run test` ✅
  - `FULL_BOOT=fast npm run test` ✅
  - `RENDERER=webgl npm run test` ✅
- Notes: Baseline green on WebGPU and WebGL2 paths. World generation ~920-1275 ms. No Emscripten native module (expected). Cloud capacity warnings pre-existing.
- Next step: #1303 — calibrate ENTITY_BASE_OFFSETS and route spawn paths through plantOnSurface.

## Iteration 1 — #1303 Calibrate ENTITY_BASE_OFFSETS and route spawn paths
- Status: Complete (100%)
- Files touched:
  - `src/world/placement-utils.ts` — documented #1303 audited offsets (all grounded archetypes keep 0 because geometry pivots are already at visual base).
  - `src/world/generation-core.ts` — routed luminous-plant spawn and seed-ring spawn through `plantOnSurface()`.
  - `src/world/generation-decorators.ts` — routed all grounded procedural extras through `plantOnSurface()`; kept direct placement only for clouds, melody_mirror, panning_pad, and vine ladders.
- Verification:
  - `npm run build:ci` ✅
  - `npm run test:wasm` ✅
  - `npm run test` ✅
  - `FULL_BOOT=fast npm run test` ✅
  - `RENDERER=webgl npm run test` ✅
- Measured offset values: all audited grounded types stay at 0 (visual base already at local y=0). `panning_pad` remains intentionally offset by +0.5 via caller.
- Next step: #1302/#1310 — add `sampleGroundNormal`, `sampleGroundFootprint`, slope alignment, and footprint-aware `plantOnSurface`.

## Iteration 2 — #1302 slope alignment + #1310 footprint sampling + debug overlay
- Status: Complete (100%)
- Files touched:
  - `src/systems/ground-system.ts` — added `registerGroundNormalData`, `sampleGroundNormal` (baked-normal primary + 3-point finite-difference fallback), and `sampleGroundFootprint`.
  - `src/world/ground-heightmap.ts` — registers baked normal data after generation.
  - `src/world/placement-utils.ts` — added `SLOPE_ALIGN_TYPES`, `FOOTPRINT_RADIUS`, `FOOTPRINT_SAMPLES`, `MAX_SLOPE_ANGLE`, `getGroundAlignedQuaternion`, and footprint-aware/slope-aware `plantOnSurface()`.
  - `src/foliage/tree-batcher.ts`, `src/foliage/arpeggio-batcher.ts`, `src/foliage/luminous-plant-batcher.ts`, `src/foliage/portamento-batcher.ts` — compose instance matrices with `getGroundAlignedQuaternion()` so stored slope tilt is applied.
  - `src/debug/ground-debug.ts` — `PlantedInstance` now stores `footprintRadius` and `normal`; `?debugHeights=1` overlay highlights the nearest wide prop with a base ring, footprint ring, and surface-normal arrow.
- Verification:
  - `npm run build:ci` ✅
  - `npm run test:wasm` ✅
  - `npm run test` ✅
  - `FULL_BOOT=fast npm run test` ✅
  - `RENDERER=webgl npm run test` ✅
- Notes: Slope alignment is capped to 45° to keep props upright. Footprint sampling uses the lowest contact point so wide props don't float on uneven ground. The debug overlay only tracks props that recorded a footprint radius.
- Next step: #1303 remaining — calibrate per-type `ENTITY_BASE_OFFSETS` using the debug overlay if any visible gaps remain, or close the grounding cluster.
