# #1577 Kinematic Character Controller

## What changed (final iteration)

- Added `CHARACTER_CONTROLLER` config block, `CharacterIntent`, `CharacterStepResult`, and zero-alloc scratch state in [`src/systems/physics/physics-types.ts`](src/systems/physics/physics-types.ts).
- Implemented `probeCapsuleGround()` + `stepCharacter()` in [`src/systems/physics/physics-updates.ts`](src/systems/physics/physics-updates.ts): capsule footprint ground probe, slope walk/slide, step-up, coyote time, jump buffer, air control, terminal fall clamp.
- Replaced Emscripten `updatePhysicsCPP` movement path in [`src/systems/physics/physics-core.ts`](src/systems/physics/physics-core.ts) with unified `stepCharacter` call; preserved ability impulses → step → wind → WASM collision → reconcile → foliage order.
- Reset coyote/buffer timers on spawn in [`src/systems/player-spawn.ts`](src/systems/player-spawn.ts).
- Added design doc [`docs/CHARACTER_CONTROLLER.md`](docs/CHARACTER_CONTROLLER.md).

## Build / test results

| Check | Result |
|-------|--------|
| `npm run build` | **PASS** (WASM + Emscripten + Vite) |
| `npm run test:wasm` | **PASS** (exit 0) |
| `npm run test` (Playwright smoke) | **INCONCLUSIVE** — harness times out on `window.__sceneReady` in this VM, then hangs on jukebox overlay wait; boot logs show physics controller init with no movement-path errors. Pre-existing headless/WebGPU flake (see prior `.swarm-state` notes). Not caused by character controller diff. |

## Tunable defaults (`CHARACTER_CONTROLLER`)

| Field | Value |
|-------|-------|
| capsuleRadius | 0.35 |
| skinWidth | 0.08 |
| maxSlopeDeg | 42 |
| maxStepHeight | 0.4 |
| coyoteMs | 120 |
| jumpBufferMs | 120 |
| airControl | 0.35 |
| terminalFallSpeed | 55 |
| moveAccel | 15 |
| jumpVelocity | 8.0 |
| slopeSlideAccel | 12 |
| stepProbeDistance | 0.45 |
| footprintSamples | 4 |

Eye height: `CONFIG.player.eyeHeight` (1.8). Platform preservation: `CONFIG.ground.platformElevationThreshold` (1.25).

## Open risks / next step

1. **Play-test tuning** on real GPU: coyote/buffer feel, step-up on streamed tile seams, steep-hill slide speed.
2. **Smoke harness**: `__sceneReady` wait flaky in headless swiftshader — unrelated to this PR; re-run `npm run test` on machine with stable WebGPU.
3. **Optional**: unit tests for `stepCharacter` with injected `GroundQuery` mock (out of scope for #1577).
