# Sky Wave Propagation Swarm State

## Completed
- [x] Added `sky_wave` block to `assets/music-bindings.json` (propagation_ms: 800, target_biomes, decay_ms: 2000)
- [x] Imported `uTwilight` from `../foliage/sky.ts` and `BeatSync` from `../audio/beat-sync.ts` in `music-reactivity.ts`
- [x] Added wave config parsing and pre-allocated module-level wave state (`_activeWave`, `_waveColor`, `_whiteColor`, `_waveDecayStartTime`)
- [x] Added `registerBeatSync` method with night-gate (`uTwilight.value > 0.1`) and `_skyMoonNoteVal > 0` check
- [x] Modified `MusicReactivitySystem.init` to optionally accept `beatSync` and auto-register callback
- [x] Wired `beatSync` into `musicReactivitySystem.init` in `src/core/main.ts`
- [x] Added per-frame propagation logic after sky/moon section with phase-based biome lerping and dawn guard
- [x] Build passes (`npm run build` exits 0)
- [x] `assets/music-bindings.json` is valid JSON
- [x] Zero `new THREE.Color()` allocations in per-frame hot path (all colors are module-level)

## In progress
- Final verification of checklist

## Blockers
- (none)
