# Swarm State — Spatial Chunk Streaming (#1546 / #1548)

**Last updated:** 2026-08-12 (iteration 1 complete, all acceptance criteria verified)

This file replaces the stale 2026-08-04 lint/typecheck ratchet content — that
campaign is done. This is a fresh save-state for the chunk-streaming feature.

## What changed

Implemented the chunk gate for the "play" boot path so Enter materializes only
the player's spawn tile before the world is playable, per the epic. Touched
only the allowed files:

- **`src/world/chunk-streamer.ts`** (NEW) — `ChunkStreamer` class. Uses
  `RegionManager` (region-manager-core.ts) for cell state bookkeeping and
  `getCellsToLoad()` for spiral-ordered load candidates. `loadSpawnPlayable(1, 80)`
  spawns the spawn chunk + 1-ring synchronously, capped at 80 entities (chunks
  that don't fit the remaining budget are skipped whole, not partially spawned,
  and picked up by the very next background pass). `update(position)` is the
  per-frame hot path — zero-allocation unless the player has actually crossed a
  chunk boundary (two `Math.floor` + equality check otherwise).
- **`src/world/map-loader.ts`** — added `getEntityById`/`getEntitiesByIds` (O(1)
  id lookup) and `loadMapChunkIndex()` (fetches `assets/map-chunks.json`,
  returns `null` on any failure — callers fall back to a bounding-box query via
  the existing `getEntitiesInBounds`).
- **`src/world/generation-core.ts`** — `generateMap()` now takes a `bootPath:
'play' | 'explore' = 'play'` param, threaded through `populateWorld()` via
  `options.bootPath` (also defaults to `'play'`). Split into
  `generateMapPlayPath()` (chunk gate) and `generateMapExplorePath()` (old
  80m-bubble + horizon-stream behavior, preserved for explicit opt-in).
  Procedural decorators (extras, gem canopy, mycelium grove, cloud
  archipelago, sky islands) are now ALWAYS enqueued via
  `queueDecoratorBootstrap()` into `globalBackgroundProcessor` instead of
  `await`ed inline — this was the actual blocking wait on both boot paths, not
  just the `waitForFullPopulation` UI flag (see "Root-cause note" below).
- **`src/core/game-loop-streaming.ts`** (NEW) + **`game-loop.ts`** — one-line
  per-frame hook: `updateStreamingPhase(player.position)`, no-op when no
  streamer is active (explore/CORE modes).
- **`tools/map-generator/build-chunk-index.ts`** (NEW) — reuses `loadMap()`
  from map-loader.ts (same id-assignment + legacy-setpiece pipeline the game
  uses at runtime) so the index's ids always match what the game will spawn.
  Buckets by 32m chunk into `assets/map-chunks.json` (`{"cx,cz": [ids],
"__meta__": {...}}`). `npm run generate:chunk-index` added to package.json.
- **`assets/map-chunks.json`** (committed) — 252 chunks, 2220 entities (2219
  raw + 1 legacy cave setpiece the runtime injects). Regenerate with
  `npm run generate:chunk-index` after any map.json change; CI should assert
  `__meta__.entityCount` matches a fresh run's `loaded.entities.length` (not
  raw `map.json.entities.length` — the runtime adds legacy setpieces on load,
  so raw count and post-normalization count can legitimately differ by a few).

`populateWorld()`'s default (`options` omitted or `bootPath` unspecified) is
now `'play'` — this is the actual behavior change: every existing call site
(start-screen.ts, untouched) gets the fast path automatically.

## Root-cause note (why boot was actually slow)

Before this change, `generateMap()` for FULL mode did:

1. Phase 1: spawn up to 300 entities within 80m, synchronously (already
   present pre-PR).
2. Phase 2: queue the rest via `streamEntitiesNear` (non-blocking, already
   fine).
3. **`await populateProceduralExtras() / populateGemCanopyCorridor() /
populateMyceliumGrove() / populateCloudArchipelago() / populateSkyIslands()`
   — all awaited inline, blocking `generateMap()`'s promise.**

Since `start-screen.ts` does `await populateWorld(...)` before calling
`markReadinessPlayable()`, step 3 was the real critical-path blocker — not
just the `mapSizeWaitsForFullPopulation` UI flag (which only ever gated
loading-screen progress-bar cosmetics for `large` map size, after
`populateWorld()` had already resolved). Moving step 3 to
`queueDecoratorBootstrap()` (background-enqueued, not awaited) is what
actually removes the blocking wait on **both** boot paths.

## Honest status

| Check                                                                                                       | Result                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm run generate:chunk-index`                                                                              | ✅ 252 chunks / 2220 entities, deterministic (re-run produces byte-identical output)                                                                                                                                                                                                                                                                                                                                      |
| `npm run build:wasm && npm run build`                                                                       | ✅ green (Emscripten step skips gracefully — no `em++` in this sandbox, pre-existing/unrelated; JS fallback engages as designed)                                                                                                                                                                                                                                                                                          |
| `npm run test:wasm`                                                                                         | ✅ 3/3                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `npm run test` (default, CORE/small map)                                                                    | ⚠️ reports FAILED, but the only `[CONSOLE ERROR]` is `net::ERR_CONNECTION_RESET` fetching `fonts.googleapis.com` — **pre-existing, confirmed identical on `origin/main`** (this sandbox's Chromium subprocess doesn't route through the outbound proxy that `curl`/Node do; `index.html`'s Google Fonts `<link>` is untouched and out of scope). `__sceneReady` is reached, jukebox UI assertion passes, no other errors. |
| `FULL_BOOT=1 npm run test` (play path, large map)                                                           | Same pre-existing font-fetch issue trips `hasError` before the `if (!hasError)`-gated population assertions in `tests/smoke-runner.mjs` even run — this harness can't currently validate FULL_BOOT in this sandbox regardless of this change. Verified manually instead (below).                                                                                                                                          |
| Manual Playwright verification (real browser, `graphics=medium&map=large`, i.e. FULL mode → play boot path) | ✅ see below                                                                                                                                                                                                                                                                                                                                                                                                              |

### Manual verification results (throwaway script, not committed)

- `[World] Play boot: spawned 75 entities in spawn chunk` — **≤ 80 ✓**
- `Enter click -> instructions hidden (playable)`: 610–2776ms across runs
  (variance from this sandbox's swiftshader software rendering) — **≤ 8s ✓**
- `__startupProfile` "Map Generation" phase: 286–874ms measured — comfortably
  fast.
- Walking (holding W, pointer-lock engaged, ~12–16m covered per run — see
  footgun below on why longer walks weren't reliably testable): foliage count
  stayed flat when the player only moved within the already-preloaded 2-chunk
  ring (correct — nothing new to load), and grew via background streaming
  without any observed **new** eviction bug. No spawn-tile entities vanished
  incorrectly after the fix below.
- No NEW console errors beyond: (a) the pre-existing font-fetch issue, and (b)
  a **pre-existing, unrelated** bug — `[World] onPlacement() failed for
"tree"/"gem_canopy_tree"/"flower"/"shrub": ReferenceError: updatedMeshes is
not defined` in `src/foliage/tree-batcher/ops.ts:551`. Confirmed via `git
diff origin/main -- src/foliage/tree-batcher/ops.ts` (empty diff — I never
  touched this file). It fires whenever tree/flower/shrub batchers flush
  pending instances, so it fires on **both** boot paths, on `main` too, any
  time FULL mode actually spawns trees in a live browser. Non-fatal (caught,
  logged, spawn recorded as failed) but means some trees/flowers are visually
  missing. Out of scope for this PR (forbidden file) — flagging for Noah.

## Bug found AND fixed during verification

**`RegionManager.getCellsToUnload()` (region-manager-lod.ts) was unsafe to
use as-is.** My first implementation of `ChunkStreamer.evictFarChunks()`
called `this.region.getCellsToUnload(cx, cz, evictRingChunks)` per the "reuse
the existing grid, don't fork it" instruction. Live-browser testing caught it
immediately: walking one single chunk over (well within the 4-ring/128m evict
radius) triggered **evicting all 25 currently-loaded chunks**, including the
spawn chunk itself.

Root cause (as far as I chased it — the file itself is out of scope for this
PR): `getCellsToUnload()` routes through `batchDistanceCull()` (WASM,
`assembly/batch.ts`), whose flag semantics are `1.0 = within range` / `0.0 =
far`, and the JS-side interpretation (`flags[i] === 0.0` → push to unload)
matches that correctly on paper — but in the live app the returned flags
don't discriminate by distance at all (every loaded cell comes back as a
candidate). `RegionManager` was **dormant** before this change (registered
nowhere in the live game loop — grepped `region-manager-core.ts` /
`region-manager.ts` usage pre-PR: only `asset-streaming-core.ts`, itself
unused by `main.ts`/game-loop). This PR is the first real caller of
`getCellsToUnload()`, so this looks like a latent, previously-unexercised bug
in `region-manager-lod.ts`, not something my diff introduced.

**Fix:** `evictFarChunks()` now does its own direct chunk-distance check in
`chunk-streamer.ts` (iterating `this.records`, not `RegionManager.cells`) and
only uses `RegionManager` for cell **state** bookkeeping (`getCell`,
`forceUnloadCell`) — not the buggy distance-cull helper. `getCellsToLoad()` is
still used for the load side; it's pure-JS spiral generation with no WASM
dependency and tested fine (background streaming grew correctly, spawn-tile
cap held). Re-verified after the fix: foliage count stayed flat (505 → 505)
across a boundary-crossing walk that shouldn't have evicted anything, vs.
losing 174 objects before the fix.

**Follow-up recommended (not done here, `region-manager-lod.ts` not in this
PR's allowed files):** root-cause `batchDistanceCull`'s flag output not
discriminating by distance in the live app — possibly a stale/detached
typed-array view over WASM memory after a `memory.grow()` elsewhere, since
`outputView` in `wasm-batch-core.ts` is allocated once and other systems
(foliage LOD culling, physics) also grow/use the same WASM instance. Anyone
calling `RegionManager.getCellsToUnload()` / `getDistantCells()` elsewhere in
the future should treat them as unverified until this is chased down.

## Design choices / eviction scope (the "known risk" the task flagged)

- **Discovery grid (`optimizedDiscovery`, WASM/AS spatial grid) has no
  unregister API** (checked `discovery-optimized.ts` — only `registerObject`
  and a full `reset()`). ChunkStreamer registers each newly-spawned object
  once (deduped by `obj.uuid`) and **never unregisters on eviction** — the
  discovery grid keeps a stale entry at that position/type after the mesh is
  despawned. Practically: if a player evicts a chunk (walks >128m away) and
  later walks back near the _exact_ old position before the mesh respawns,
  they could see a discovery toast for something not visually there yet. Cosmetic,
  not a crash. A real fix needs an `unregisterObject(id)` added to
  `discovery-optimized.ts` (not in this PR's allowed files).
- **Eviction is conservative on batched (InstancedMesh) entities.** Only
  `mushroom` type entities have a public removal API
  (`MushroomBatcher.removeInstance`). Every other batched type (flowers,
  arpeggio_fern, portamento_pine, gem_canopy_tree, glass_mushroom,
  prism_rose_bush — see `isKnownBatchedType()` in chunk-streamer.ts, mirrors
  `safeAddFoliage`'s local `isBatched` check since that logic isn't exported)
  and `cave` (WASM collision + weatherSystem registration, no removal) are
  classified `'never'` — loaded permanently once streamed in, never evicted.
  This trades a bounded amount of "world never fully unloads" memory for
  correctness (no corrupted/leaked InstancedMesh indices). Non-batched
  individual-mesh entities (most musical-flora types: panning_pad,
  instrument_shrine, silence_spirit, kick_drum_geyser, etc.) evict fully:
  removed from `foliageGroup` + every tracking array in `state.ts` +
  `vineSwings` (for `swingable_vine`), disposed via `safeRemoveAndDispose`,
  and the entity id is freed from `spawnedIds` so walking back respawns it.
- **`obstaclesData` (generation-utils.ts) is dead code** — verified via
  repo-wide grep: written by `safeAddFoliage` but read nowhere. Actual
  obstacle/collision consistency comes entirely from `populatePhysicsGrids()`
  (physics-core.ts), which rebuilds fully from `animatedFoliage` /
  `foliageTraps` / `foliageGeysers` / `foliagePortamentoPines` /
  `foliagePanningPads` every call — so as long as ChunkStreamer keeps those
  arrays in sync on evict (it does) and calls `populatePhysicsGrids()` after
  (it does, via a `physicsDirty` flag checked once per frame in `update()`),
  physics stays consistent. No separate WASM obstacle buffer to desync.

## Measured Play-path boot timings (from manual verification, see above)

- Map Generation phase: **286–874ms** (target: contributes to ≤8s total)
- Enter → instructions-hidden (playable): **610–2776ms** (target: ≤8s) ✓
- Entities spawned synchronously: **75** (target: ≤80) ✓

## NEXT STEP

1. Re-run the manual Playwright verification (see "throwaway script" pattern
   above — not committed, was `verify-play-path.tmp.mjs` at repo root, deleted
   after use) in an environment with a real GPU / non-headless Chromium to get
   a longer, stable walk (this sandbox's headless swiftshader crashed the tab
   during a 30s continuous-walk stress test — separate from the eviction fix,
   looked like general headless-WebGPU instability per the same caveat
   `tests/smoke-runner.mjs` already documents for FULL_BOOT mode). Specifically
   want to confirm: walking a full 200m crosses multiple chunk boundaries
   cleanly, evicts far chunks without frame stalls >50ms, and doesn't leak.
2. Consider whether `tests/smoke-runner.mjs`'s FULL_BOOT flow should have its
   `hasError` gate loosened to not block population assertions on unrelated
   network-resource failures — out of scope for this PR (forbidden file), but
   worth a ticket; it currently makes FULL_BOOT smoke testing unusable in any
   sandboxed/restricted-network CI runner.
3. File a ticket for the pre-existing `updatedMeshes is not defined` bug in
   `src/foliage/tree-batcher/ops.ts:551` (tree/flower/shrub batcher instance
   flush) — real bug, unrelated to this PR, currently causing silent spawn
   failures for a meaningful fraction of trees/flowers/shrubs on every FULL
   boot.
4. File a ticket to root-cause `RegionManager.getCellsToUnload()` /
   `batchDistanceCull` flag semantics in `region-manager-lod.ts` — see "Bug
   found AND fixed" above. My workaround (direct JS distance check in
   chunk-streamer.ts) is safe and shipped, but the underlying LOD helper is
   still broken for any future caller.
5. If Noah wants explore-path (`bootPath: 'explore'`) actually reachable from
   the UI, that's `src/core/main/start-screen.ts` (forbidden in this PR,
   parallel Copilot slice #1547) — the plumbing (`options.bootPath`) is ready
   on this end.

## Footguns for whoever picks this up

- `ChunkStreamer` is a per-boot singleton (`setActiveChunkStreamer`/
  `getActiveChunkStreamer` in chunk-streamer.ts). `generateMap()` calls
  `setActiveChunkStreamer(null)` at the top (disposes any previous instance)
  before branching on `bootPath` — important for world regeneration
  (`btn-map-*` → re-click Enter) not to leak the old streamer or double-drive
  `update()`.
- `loadSpawnPlayable()`'s 80-entity cap skips a chunk **whole** if it doesn't
  fit the remaining budget, rather than partially spawning it — this keeps
  chunk state binary (UNLOADED/LOADED, no partial state to track) at the cost
  of the boot batch sometimes being noticeably under 80 even when a couple
  more entities would technically fit. Acceptable given the target is "≤80",
  not "as close to 80 as possible."
- Background chunk-entity tasks are enqueued **one per entity**
  (`chunk_stream_${chunkKey}_${id}`), matching the existing `map_stream_*`
  convention from the explore path — not batched into one task per chunk.
  This keeps `globalBackgroundProcessor`'s per-task time-budgeting granular
  (a single slow tree spawn only overruns by itself, doesn't drag a whole
  chunk's worth of entities into one unbudgeted block).
