{
  "name": "candy_world",
  "version": "1.0.0",
  "description": "A 3D world of rudimentary, but sharp graphically nature - featuring smooth, glossy shapes in a pastel candy-colored landscape.",
  "type": "module",
  "scripts": {
    "dev": "bash dev.sh",
    "preview": "vite preview",
    "build:wasm": "asc assembly/index.ts --outFile src/wasm/candy_physics.wasm --textFile src/wasm/candy_physics.wat --sourceMap --shrinkLevel 0 --optimize --converge --optimizeLevel 3 --enable simd --enable bulk-memory --initialMemory 384 --target wasm-gc --enable reference-types --enable gc --exportRuntime --bindings esm",
    "build:emcc": "bash emscripten/build.sh",
    "optimize": "bash tools/optimize.sh",
    "build": "npm run build:wasm && npm run build:emcc && npm run optimize && vite build",
    "test": "python3 verify.py",
    "test:wasm": "node verification/test_wasm_loader.mjs",
    "verify:emcc": "node verification/verify_emcc_exports.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "three": "^0.171.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/three": "^0.171.0",
    "assemblyscript": "^0.27.37",
    "typescript": "^5.3.3",
    "vite": "^5.0.0",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.5.0"
  }
}
