# Dependencies
node_modules/
# Build output
dist/
build/
emsdk
# Emscripten intermediates (libomp.a is intentionally tracked — see emscripten/REFACTORING_README.md)
emscripten/*.o
emscripten/penmp
emscripten/penmp.wasm
*.o
*.bak
# AssemblyScript physics WASM binaries (built via npm run build:wasm).
# Tracked glue: src/wasm/candy_physics.js + candy_physics.d.ts (asc --bindings esm).
*.wasm
*.wat
*.map
# Emscripten / Vite production WASM (built via npm run build:emcc)
public/candy_native*
public/candy_physics*
# Logs
*.log
npm-debug.log*
# OS files
.DS_Store
Thumbs.db
# IDE
.vscode/
.idea/
*.swp
*.swo
*~
# Vite
.vite/
verify_mod_player.md
test_build/
.aider*
package-lock.json
public/js/libopenmpt.js

# Release tooling
candy-world-*.zip
candy-world-*.tar.gz
